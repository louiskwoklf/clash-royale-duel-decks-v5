{
  "name": "clash-royale-duel-decks-v5",
  "version": "0.1.0",
  "private": true,
  "description": "Build four Clash Royale war decks from Safari-imported RoyaleAPI searches.",
  "scripts": {
    "build": "node src/build-site.js",
    "test": "npm run build && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
